Ext.getCmp('viewport-east').setVisible(false);

var center = Ext.getCmp('viewport-center');
center.removeAll();

center.add(new Ext.TabPanel(
	{
		id: 'page-edit-tab',
		resizeTabs: false,
		minTabWidth: 115,
		tabWidth:135,
		enableTabScroll:true,
		defaults: {
			autoScroll:true
		},
		tabPosition: 'top'
	}
));

var spot = new Ext.ux.Spotlight({
	easing: 'easeOut',
	duration: .3
});
